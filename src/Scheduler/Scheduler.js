import InputContainer from './UserInput/InputContainer';
import "./Scheduler.css";

const Scheduler = () => {
  return (
    <div className="scheduler">
        <InputContainer />
    </div>
  );
};

export default Scheduler;
